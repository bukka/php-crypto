// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("crypto", "for crypto support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("crypto", "enable crypto support", "no");

if (PHP_CRYPTO != "no") {
	EXTENSION("crypto", "crypto.c");
}

